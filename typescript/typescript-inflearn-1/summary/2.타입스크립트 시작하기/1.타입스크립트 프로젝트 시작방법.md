# typescript라이브러리 설치
- 타입스크립트 파일의 확장자는 `.ts`이다. 이 파일을 만들었다해서 타입스크립트 실행할수있는건아니고, 타입스크립트파일을 자바스크립트 파일로 컴파일을 해야한다. 컴파일을 하는방법은 `typescript`라는 라이브러리를 이용해서 할수있다. 난 글로벌하게 설치하여 `tsc`라는 글로벌 명령어로 타입스크립트파일을 자바스크립트 파일로 컴파일했다.
- `tsc index.ts`하니 `index.js`파일이 생성되었다. 이걸 **컴파일**이라 부른다.